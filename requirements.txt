-r constraints.txt
dlib-bin==19.24.6           # Use pre-built version of dlib that does not require CMake
streamlit==1.22.0  # Specify your Streamlit version
Flask==2.3.3       # Include if needed
Flask-SocketIO==5.3.0  # Include if using Flask-SocketIO
face_recognition==1.3.0  # Check compatibility with dlib-bin
opencv-python==4.6.0.66  # OpenCV for image processing
numpy>=1.26.0      # Ensure compatibility with Python 3.12